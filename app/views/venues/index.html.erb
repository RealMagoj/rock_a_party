<h1>Rock-a-Party</h1>
<% if current_user %>
  <%= link_to "Add Venue", new_venue_path %>
<% end %>
<% if @venues.length > 0 %>
  <% @venues.each do |venue| %>
    <h2><%= link_to venue.name, venue_path(venue) %></h2>
  <% end %>
<% end %>